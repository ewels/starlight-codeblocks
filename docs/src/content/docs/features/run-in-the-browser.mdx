---
title: Run in the browser
description: Add a Run button that runs the example in the browser and shows the output under the block.
---

import Example from '../../../components/Example.astro';

export const basic = `
\`\`\`py runnable
from statistics import mean, stdev

reads = [1520, 1610, 1480, 1575]
print(f"mean {mean(reads):.0f}, sd {stdev(reads):.1f}")
\`\`\`
`;

<Example code={basic} />

export const js = `
\`\`\`js runnable
const sizes = [3, 1, 4, 1, 5];
console.log('Total:', sizes.reduce((a, b) => a + b, 0));
console.error('A warning');
\`\`\`
`;

<Example code={js} />

export const loop = `
\`\`\`js runnable
while (true) {}
\`\`\`
`;

<Example code={loop} />

export const error = `
\`\`\`py runnable
counts = {"a": 3, "b": 0}
print(counts["a"] / counts["b"])
\`\`\`
`;

<Example code={error} />

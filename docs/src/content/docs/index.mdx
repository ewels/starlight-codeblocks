---
title: starlight-codeblocks
description: A Starlight plugin that adds focus, line states, annotations, API links and 20 more features to the code blocks of a site.
template: splash
hero:
  tagline: More ways to explain code in Starlight code blocks.
  actions:
    - text: Get started
      link: /starlight-codeblocks/getting-started/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/ewels/starlight-codeblocks
      icon: external
      variant: minimal
---

import { CodeSteps, Scrollycoding, Step } from 'starlight-codeblocks/components';
import Feature from '../../components/Feature.astro';
import FeatureCarousel from '../../components/FeatureCarousel.astro';

Starlight renders every code block with Expressive Code. The plugin adds 24 features to those code blocks, such as focus, line states, annotations, links to API docs and runnable examples. Each feature starts when a code block uses its attribute or its directive, so existing pages do not change.

## Features

Select a feature to see its example. Until you select one, the example changes every 7 seconds. Hover over the example, or move keyboard focus into it, to stop the change while you read.

<FeatureCarousel>

<Feature page="features/comment-notation">

```ts title="config.ts" wordDiff=false
export const config = {
  port: 3000, // [!code --]
  port: Number(process.env.PORT ?? 3000), // [!code ++]
  host: 'localhost', // [!code highlight]
};
```

</Feature>

<Feature page="features/annotations">

```yaml title=".github/workflows/test.yml"
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ["3.12", "3.13"]  # [!annotate] One job per version, run in parallel.
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v6  # [!annotate] Installs `uv` and caches its downloads between runs.
      - run: uv run pytest
```

</Feature>

<Feature page="features/footnotes">

```py title="app.py" apiLinks=false
from flask import Flask

# [!ref] Creates the application object.
app = Flask(__name__)

# [!ref] Runs this function for `GET /health`.
@app.get("/health")
def health():
    return {"ok": True}
```

</Feature>

<Feature page="features/inline-callouts">

```js
const controller = new AbortController();
// [!callout /signal/] Lets `controller.abort()` cancel the request.
const res = await fetch(url, { signal: controller.signal });
const data = await res.json();
```

</Feature>

<Feature page="features/side-by-side-annotations" tall>

```py title="report.py" annotations="side" apiLinks=false
import csv
from collections import Counter


def read_rows(path):
    with path.open() as fh:  # [!annotate] Opens the file and closes it when the block ends.
        return list(csv.DictReader(fh))  # [!annotate] Each row becomes a dict keyed by the header line.


def summarise(rows, key):
    return Counter(r[key] for r in rows)  # [!annotate] Counts the rows for each value in the column `key`.
```

</Feature>

<Feature page="features/scrollycoding" tall>

<Scrollycoding>

```js title="server.js"
import express from 'express';

const app = express();
app.use(express.json());

app.listen(3000);
```

<Step focus="1">Import Express. Nothing else is needed for a small server.</Step>
<Step focus="3-4">Create the app object, and parse JSON request bodies.</Step>
<Step focus="6">Start listening on port 3000.</Step>

</Scrollycoding>

</Feature>

<Feature page="features/focus">

```js title="src/config.js" focus={4-7}
import { defineConfig } from './lib.js';

export default defineConfig({
  cache: {
    dir: '.cache',
    maxAge: 3600,
  },
  retries: 2,
  verbose: false,
});
```

</Feature>

<Feature page="features/line-states">

```py title="loop.py" info={1} apiLinks=false
import sys

for name in sys.argv[1:]  # [!code error] SyntaxError: expected ':'
    print(name)

count = len(sys.argv)  # [!code warning] Includes the script name
```

</Feature>

<Feature page="features/code-mentions">

The [base case](#mention:base) stops the recursion. The [recursive step](#mention:step) calls the function again with a smaller number.

```py title="factorial.py"
def factorial(n):
    if n == 0:  # [!mention base]
        return 1  # [!mention base]
    return n * factorial(n - 1)  # [!mention step]
```

</Feature>

<Feature page="features/word-level-diff">

```diff lang="js"
-const timeout = 5000;
+const timeout = options.timeout ?? 5000;
const client = createClient({
-  retries: 3,
+  retries: options.retries,
});
```

</Feature>

<Feature page="features/token-transitions">

<CodeSteps>

```js title="server.js" step="Create the app"
const app = express();

app.listen(3000);
```

```js title="server.js" step="Parse JSON bodies"
const app = express();
app.use(express.json());

app.listen(3000);
```

```js title="server.js" step="Add a health route"
const app = express();
app.use(express.json());

app.get('/health', (req, res) => {
  res.json({ ok: true });
});

app.listen(3000);
```

</CodeSteps>

</Feature>

<Feature page="features/hidden-lines">

```py title="summary.py" hidden={1-3,6-7} apiLinks=false
import json
from pathlib import Path

config = json.loads(Path("config.json").read_text())
for key, value in config.items():
    if key.startswith("_"):
        continue
    print(f"{key} = {value}")
```

</Feature>

<Feature page="features/expandable-blocks">

```py title="report.py" expandable={8} apiLinks=false
import csv
import sys
from collections import Counter
from pathlib import Path


def read_rows(path: Path) -> list[dict]:
    with path.open() as fh:
        return list(csv.DictReader(fh))


def summarise(rows: list[dict]) -> Counter:
    return Counter(row["status"] for row in rows)


def main() -> None:
    rows = read_rows(Path(sys.argv[1]))
    counts = summarise(rows)
    for status, n in counts.most_common():
        print(f"{status:<10} {n:>5}")


if __name__ == "__main__":
    main()
```

</Feature>

<Feature page="features/visible-whitespace">

```make title="Makefile" whitespace
build:
	cargo build --release

test:
    cargo test  # Recipes must start with a tab
```

</Feature>

<Feature page="features/colourised-brackets">

```js brackets
const title = document.title;
const slug = encodeURIComponent(kebabCase(trim(title.toLowerCase())));
```

</Feature>

<Feature page="features/inline-code-highlighting">

Call `await fetch(url){:js}` and check `res.ok{:js}` before you read the body.

</Feature>

<Feature page="features/token-links">

```py apiLinks=false
import numpy as np

# [!link /linspace/ https://numpy.org/doc/stable/reference/generated/numpy.linspace.html]
x = np.linspace(0, 1, 50)
y = np.sin(2 * np.pi * x)
```

</Feature>

<Feature page="features/api-auto-linking">

```py title="report.py"
import json
from pathlib import Path

run = json.loads(Path("run.json").read_text())
print(run["status"])
```

</Feature>

<Feature page="features/line-permalinks">

```yaml title="config.yml" id="cfg"
server:
  host: 0.0.0.0
  port: 8080
cache:
  dir: .cache
  max_age: 3600
logging:
  level: info
```

</Feature>

<Feature page="features/code-switcher">

:::code-switcher{sync="lang"}
```py apiLinks=false
import json

with open("config.json") as fh:
    config = json.load(fh)
```
```js
import { readFile } from 'node:fs/promises';

const config = JSON.parse(await readFile('config.json', 'utf8'));
```
:::

</Feature>

<Feature page="features/fill-in-placeholders">

```sh placeholder="YOUR_TOKEN,WORKSPACE_ID"
curl -H "Authorization: Bearer YOUR_TOKEN" \
  https://api.example.com/workspaces/WORKSPACE_ID/runs
```

</Feature>

<Feature page="features/smart-shell-copy">

```sh frame="terminal"
$ uv tool install ruff
Resolved 1 package in 180ms
Installed 1 executable: ruff
$ ruff check src/ \
    --fix
Found 3 errors (3 fixed, 0 remaining).
```

</Feature>

<Feature page="features/open-in-playground">

```ts playground="typescript"
type Status = 'queued' | 'running' | 'done';

function label(s: Status): string {
  return s === 'done' ? 'Finished' : s;
}

console.log(label('done'));
```

</Feature>

<Feature page="features/run-in-the-browser">

```js runnable
const sizes = [3, 1, 4, 1, 5];
const total = sizes.reduce((sum, size) => sum + size, 0);
console.log('Total:', total);
console.error('Sizes of 1 are deprecated.');
```

</Feature>

</FeatureCarousel>

## Install

Add the package to the site:

:::code-switcher{sync="pm"}
```sh label="npm"
npm install starlight-codeblocks
```
```sh label="pnpm"
pnpm add starlight-codeblocks
```
```sh label="Yarn"
yarn add starlight-codeblocks
```
:::

Then add `codeblocks()` to the Starlight plugins in `astro.config.mjs`:

```js title="astro.config.mjs" ins={9}
import { defineConfig } from 'astro/config';
import starlight from '@astrojs/starlight';
import codeblocks from 'starlight-codeblocks';

export default defineConfig({
  integrations: [
    starlight({
      title: 'My docs',
      plugins: [codeblocks()],
    }),
  ],
});
```

The [getting started](/starlight-codeblocks/getting-started/) page has the full steps.

## Extend and reference

- [Extend](/starlight-codeblocks/extend/write-an-api-link-adapter/): your own API link adapters, playgrounds and runtimes.
- [Reference](/starlight-codeblocks/reference/options/): every option, attribute, directive and style setting.

## Markdown versions of the pages

Each page of these docs has a Markdown version at the same address with `.md` at the end, such as [getting-started.md](/starlight-codeblocks/getting-started.md). The buttons under the title of each page copy it as Markdown or open it in an AI assistant. Code blocks in the Markdown keep their fence lines, so an AI assistant sees the attributes and directives as you write them. [llms.txt](/starlight-codeblocks/llms.txt) lists every page, and [llms-full.txt](/starlight-codeblocks/llms-full.txt) has every page in one file.

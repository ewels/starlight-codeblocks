# Progress

Update this file after each step in `PLAN.md`. Use one of these states: not started, in progress, done, blocked. When the plan is complete, add a summary at the top: what is done, what is blocked and why, and what a person needs to check first.

| Step | State | Notes |
|---|---|---|
| 0.1 If main has no commits, commit the design pack, AGENTS.md,… | done | main already had the design pack commits (f624beb, 7f19da9), pushed. |
| 0.2 Create the branch main, and push it | done | Stale row: main exists and is the work branch (see DECISIONS.md). |
| 1.1 Work through ARCHITECTURE-QUESTIONS.md with small throwaway… | done | Real Astro 7.3.5 / Starlight 0.42.4 / EC 0.44.2 / Sätteri 0.10.5 site in the scratchpad, not committed. |
| 1.2 Write the answers to design/ARCHITECTURE.md | done | All nine answered. No step blocked. Phase 10 is feasible (Q5). Code switcher is a directive (Q2). Step 3.3 must also settle client files for preset-only sites (Q4). |
| 2.1 Create the pnpm workspace: packages/<name> (TypeScript, ESM, built… | done | pnpm workspace, tsdown package with `.` and `./expressive-code` exports, Starlight docs site. Biome and TypeScript 6 (see DECISIONS.md). Docs build is warning-free. |
| 2.2 Set up Vitest with a render helper that runs a Markdown code block… | done | `test/render.ts`: `render(markdown)` renders one fenced block with `ExpressiveCode` (default plugins, so frames and the copy button are there) plus `pluginCodeblocks()`, and returns `{ html, copyText }`. Extend it with plugin options in 3.1. |
| 2.3 Set up Playwright against a production build of the docs site,… | done | Playwright in `docs/` (Chromium). Projects: desktop-light, desktop-dark, phone-light, phone-dark (360 px), reduced-motion (desktop, dark). `astro preview --ignore-lock` serves `dist/`; root `pnpm test:e2e` builds first, `pnpm --filter docs test:e2e` reuses the build. |
| 2.4 Write scripts/lint-docs.mjs from the rules in WRITING-STYLE.md,… | done | `scripts/lint-docs.mjs` with `node --test` tests in `scripts/lint-docs.test.mjs`. Pass file paths to lint other files. Skips "key" and "essential" (see DECISIONS.md). |
| 2.5 Add a CI workflow that runs lint, unit tests, both builds and the… | done | `.github/workflows/ci.yml`: one job on Node 22 (lint, lint:docs, test, docs:build, Playwright Chromium). Actions pinned to SHAs with actions-up; zizmor clean. Not run yet (needs a push). The GitHub Pages deploy workflow from DOCS-SITE.md is now written too: `.github/workflows/deploy-docs.yml`, added and deferred from this step per DOCS-SITE.md (see DECISIONS.md, "GitHub Pages deploy workflow added"). GitHub Pages itself still needs enabling with source "GitHub Actions" before it can deploy. |
| 2.6 Build the docs site's page skeleton from DOCS-SITE.md, including… | done | All sidebar pages from DOCS-SITE.md as "Not yet written" placeholders, sidebar in order, `starlight-links-validator` (ignores `#mention:`), `Example` component (checked by hand on a temporary page). Plugin features in `Example` need the `<Code>` route from step 3.1. |
| 3.1 Options: the codeblocks(options) Starlight plugin, the… | done | `src/options.ts` (types, validation, `optionsReference`), `src/registry.ts`, `src/index.ts` (Q1 route: hidden plugins + `ec-config` override, build error when `ec.config.mjs` has `plugins` without the preset), `src/integration.ts`. `render(markdown, options)` returns `{ html, copyText, warnings }`. Root `lint` and `test` build first (fixes CI on a fresh clone). |
| 3.2 The range parser and the comment notation parser, with the comment… | done | `ranges.ts` (`parseRange`), `comments.ts` (syntax map), `notation.ts` (`parseLine`, `parseNotation`, `pluginNotation`, `getDirectives`), `core.ts` (`resolveRange`, `warn`, `fail`, `numberedLines`), `inline-markdown.ts`. `[!code highlight]`, `++` and `--` work. Expressive Code ranges count visible lines (see DECISIONS.md). |
| 3.3 The client module loader, so that each page loads only the modules… | done | `src/client-modules.ts` (`readClientModules`, `loaderSource`, `clientJsModules`), client Vite plugins in `src/integration.ts`, per-module tsdown builds, `src/client/shared/position.ts` (`place`). Preset-only sites get an inline loader with `blob:` imports (see DECISIONS.md). Checked by hand in `astro dev` and `astro preview` with a temporary module; Playwright tests on a routed origin cover both loader modes and both positioning paths. No feature module exists yet, so the docs pages load no client JavaScript. |
| 3.4 Style settings and theme handling: the shared tokens, light and… | done | `src/expressive-code/styles.ts`: `codeblocks` style settings group (dark and light pairs), base styles for `scb-float`, `scb-sr-only`, `scb-no-print`, focus ring and reduced motion. Prefix `scb`. Contrast test in `test/styles.test.ts`. |
| 3.5 Docs: the getting started page, the configuration reference page… | done | Getting started, Configuration, Comment notation (feature template), Options and Directives reference pages (tables generated from the source). Playwright test for the comment notation examples, including the copied text. `pnpm lint:docs` passes; pages read against the WRITING-STYLE checklist. ARCHITECTURE.md has a "How to add a feature" section. |
| 4.1 Focus | done | `src/expressive-code/focus.ts`, CSS only (no client module). `tabindex` goes on `pre > code` (DECISIONS.md). Unit tests, Playwright tests (pointer, keyboard, reduced motion, copy), docs page. Screenshots match the mockup in both themes. |
| 4.2 Line states, including custom states | done | `src/expressive-code/line-states.ts`: built-in and custom states, messages as labels, hidden prefixes, derived colours with a contrast test. Name rules in `options.ts`. Docs site defines `todo`. Playwright tests check the copied text and a manual selection. ARCHITECTURE.md guide updated with what 4.1 and 4.2 taught. |
| 4.3 Word-level diff | done | `src/expressive-code/word-diff.ts`: pairs `del`/`ins` runs by reading text-markers' own line annotations, tokenises with a word/whitespace/punctuation split, highlights the LCS-changed ranges with `ExpressiveCodeAnnotation`, no client module. Covers `diff` blocks, `ins`/`del` attributes and `[!code ++/--]`. Unit tests, contrast test, docs page. |
| 4.4 Visible whitespace | done | `src/expressive-code/whitespace.ts`: leading-only or `whitespace="all"`, dot and arrow glyphs via an `aria-hidden` marker element over the real character, no client module. Found a platform issue: `astro-expressive-code` expands tabs to spaces by default. First fixed only in the docs site's own config; `codeblocks()` now sets `tabWidth: 0` itself for every site, unless the site already chose its own (see DECISIONS.md, "codeblocks() defaults tabWidth to 0"). Unit tests, docs page. |
| 4.5 Colourised brackets | done | `src/expressive-code/brackets.ts`: language-agnostic string/comment scanner pairs brackets by depth, `ExpressiveCodeAnnotation` per bracket. First feature with a client module (`src/client/brackets.ts`, hover outline of the partner, 0.29 kB gzipped, well inside the 3 kB budget); confirmed the loader end to end after finding a missing `jsModules: clientJsModules` wiring. Unit tests, contrast test, Playwright hover/reduced-motion tests, docs page. Fix: the annotation first wrapped the Shiki token span, whose own colour overrode the bracket colour, so brackets rendered in their normal colour. It now unwraps the token; a Playwright test checks the drawn colour of each depth. |
| 4.6 Hidden lines | done | `src/expressive-code/hidden-lines.ts`: `hidden={range}` and `[!code hide]` hide lines behind a `scb-hidden-marker`, grouped into runs with `aria-controls` to the line ids; a `scb-btn` title bar toggle (forced visible with `:has()` when the block has no title) shows every run. Client module `src/client/hidden-lines.ts` (0.50 kB gzipped) toggles classes only; the copy button is untouched, so hidden lines still copy (SPEC 5). No dedicated style settings: the marker line and the open-line tint use `color-mix()` against the shared `codeblocks.mutedForeground`/`codeForeground`/`codeBackground` tokens. Added the shared `scb-btn` utility to `styles.ts` for small title-bar/under-code buttons. Unit tests, Playwright tests (pointer, keyboard, reduced motion, copy), docs page, screenshots checked against the mockup in both themes. |
| 4.7 Expandable blocks | done | `src/expressive-code/expandable.ts`: `expandable`/`expandable={N}` mark a block with `data-scb-expandable`, skip blocks where fewer than 3 lines would hide. All collapsing happens in the client module (`src/client/expandable.ts`, 0.41 kB gzipped): it sets `hidden="until-found"` on the tail lines and listens for `beforematch`, so the SSR output has no collapsed markup at all and the block shows in full without JavaScript (SPEC 6.14). The fade and the "Show all/fewer" bar (`scb-btn`) are gated behind `@media (scripting: enabled)`/`(scripting: none)`, the same technique Expressive Code's own copy button uses. Unit tests, Playwright tests, docs page, screenshots checked against the mockup in both themes. |
| 5.1 Inline callouts | done | `src/expressive-code/callouts.ts`: `[!callout /text/]` renders a `role="note"` bubble before the target line, arrow at the middle of the match in `ch` (tabs to multiples of 8). No client module. Width capped against the visible block with container units; on narrow blocks the bubble and arrow clamp inside the block (DECISIONS.md). Runs after hidden lines in the preset. Unit and Playwright tests (arrow position, no sideways scroll at 360 px, copy, `user-select`). Docs page. `lint-docs.mjs` now skips `export const` example literals, whose `#` comment lines read as headings. |
| 5.2 Annotations | done | `src/expressive-code/annotations.ts`: numbered `popovertarget` buttons with build-time anchor names, popover after each button, print-only numbered list. Client module `src/client/annotations.ts` is only the `place()` fallback. Unit tests; Playwright tests for pointer, keyboard, Escape, one-open-at-a-time, phone viewport, copy, reduced motion, print and JavaScript turned off. Docs page. |
| 5.3 Footnotes, including the sticky option | done | `src/expressive-code/footnotes.ts`: `[!ref]` badges (links) and a numbered list in the block frame, `footnotes="sticky"`/`"static"` and the `footnotes.sticky` option. Client module `src/client/footnotes.ts`: one document click listener for the highlight and scrolling. Unit tests with a contrast test; Playwright tests for pointer, keyboard, scrolling, sticky position, copy, reduced motion and JavaScript turned off. Docs page. |
| 5.4 Side-by-side annotations | done | Part of `annotations.ts`: `annotations="side"` wraps the frame and a notes list in a container; two columns from 600 px (not 640, see DECISIONS.md), sticky below the Starlight header, list under the block on phones. `src/client/annotations.ts` adds the hover and focus link and the too-tall check. Unit and Playwright tests (layout at desktop and 360 px, sticky, hover both ways, keyboard, reduced motion, JavaScript turned off). Docs page; annotations page links it. |
| 5.5 Docs: the guide that helps authors choose between callouts,… | done | `guides/choose-an-annotation-style.mdx`: a comparison table and one example of each style with the same code and notes, then rules for choosing. |
| 6.1 Smart shell copy | done | `src/expressive-code/shell-copy.ts`, no client module. Prompts leave the code in `preprocessCode` and come back as `span.scb-shell-prompt`; output lines lose token colours; the copy button's `data-code` holds the commands and its title is "Copy commands". Found that `instanceof` on Expressive Code classes fails in `<Code>` (ARCHITECTURE.md). Unit tests with a contrast test, Playwright tests (keyboard, pointer, computed colours, manual selection), docs page; the docs site adds a `PS> ` prompt. |
| 6.2 Open in playground, with the two built-in playgrounds and the… | done | `src/expressive-code/playground.ts`, no client module. Built-in `typescript` (lz-string, a new dependency the spec names) and `rust`; custom `url` playgrounds render a link, `post` playgrounds a form with hidden fields, both `target="_blank"`. New `addTitleBarControl()` in `core.ts` and a shared `.scb-tools` group (hidden lines moved to it); `scb-btn` restyled to the mockup's `.tool`. The docs site adds Python Tutor (`url`) and StackBlitz (`post`) as live examples. Unit tests, Playwright tests (pointer, keyboard, focus ring, form post, placement, JavaScript off), docs page and the "Add a playground" extend page. |
| 6.3 Token links | done | `src/expressive-code/token-links.ts`, no client module. `[!link /text/ url]` (own line, stackable) wraps the first match in `a.scb-link`, keeping token colours. The notation parser reads only the first `/…/` as literal, so site-relative URLs work; `codeblocks()` puts Astro's `base` in the registry for them. Unit tests with a contrast test, Playwright tests (colours, hover, keyboard, base, copy), docs page. |
| 6.4 Fill-in placeholders, including playground links | done | `src/expressive-code/placeholders.ts` and client module `src/client/placeholders.ts` (0.97 kB gzipped): fields inside the token span, sync across the page and later pages, Escape clears, `storage` local/session/none. The copy text, custom URL playgrounds and form fields get the values by text replacement; the TS Playground link is rebuilt by `src/client/playground.ts` (1.9 kB gzipped, with lz-string), loaded only when a block has both. Client builds now bundle their dependencies. Runnable code (phase 11) must read the copy button's `data-code` to get the values. Unit tests, Playwright tests (typing, width, Escape, reload, colour, TS link, custom playgrounds on a routed page, JavaScript off), docs page. |
| 7.1 Line permalinks | done | `src/expressive-code/permalinks.ts` and client module `src/client/permalinks.ts`: `id` blocks get linked line numbers in their own gutter, line ids, click/Shift-click/keyboard selection with `history.replaceState`, highlight and scroll from the address on load and `hashchange`, opens hidden runs. Hidden-lines marker and callout arrows now add `--scb-gutter`. First Sätteri plugin (`src/satteri/index.ts`) warns about duplicate ids. Unit tests (with the line numbers plugin), Playwright tests (pointer, keyboard, load, hash change, hidden line, alignment, manual copy, no JavaScript), docs page, screenshots checked against the mockup in both themes. |
| 7.2 Code mentions | done | `src/expressive-code/mentions.ts` and client module `src/client/mentions.ts`: tagged lines, pairing by document order and section, hover/focus highlight with a 0.42 fade, scroll on select, `aria-describedby`. The Sätteri plugin turns unmatched links into text with a warning. `Example` renders live Markdown from its slot, checked against the source by `scripts/examples.test.mjs`. Unit tests (EC plugin and Sätteri check), Playwright tests (hover, keyboard, description, scroll, shared lines, underline, reduced motion, no JavaScript), docs page with the links validator setting, screenshots checked against the mockup in both themes. |
| 7.3 Code switcher | not started | Sätteri directive route confirmed (ARCHITECTURE.md Q2). |
| 8.1 Token transitions | not started | |
| 8.2 Scrollycoding | not started | |
| 9.1 The adapter interface, the hover card and the build-time cache for… | not started | |
| 9.2 The Python adapter, with the standard library inventory and… | not started | |
| 9.3 The Nextflow adapter, with the bundled channel and operator map… | not started | |
| 9.4 Docs: the feature page and a guide to writing an adapter | not started | |
| 10.1 Inline code highlighting, through the hook the spike found | not started | Hook found: async mdast inlineCode visitor (ARCHITECTURE.md Q5). Not blocked. |
| 11.1 The runtime interface, the Run button and the output panel | not started | |
| 11.2 The Pyodide runtime, in a web worker | not started | |
| 11.3 Docs: the feature page and a guide to adding a runtime | not started | |
| 12.1 Complete every docs page | not started | |
| 12.2 Write the migration guide for VitePress users | not started | |
| 12.3 Write the package README: what it is, the one-line install and… | not started | |
| 12.4 Fill in package.json: description, keywords (include withastro,… | not started | |
| 12.5 Final review | not started | |
| 12.6 Return to any blocked steps | not started | |
